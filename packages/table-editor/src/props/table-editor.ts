export default {
  modelValue: Array,
  // 默认最小行数
  defaultRow: {
    type: Number,
    default: 15
  },
  // 默认最小列数
  defaultCol: {
    type: Number,
    default: 15
  },
  minWidth: {
    type: Number,
    default: 70
  },
  minHeight: {
    type: Number,
    default: 25
  }
}
